{
    "authenticationMethod": "route",
    "routes": [
        {
            "source": "/backend-server/(.*)",
            "destination": "backend-api",
            "authenticationType": "xsuaa"
        }
    ],
    "welcomeFile": "/fakeapprouterui"
}